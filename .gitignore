# ── PlatformIO build output ───────────────────────────────
.pio/
.pioenvs/
.piolibdeps/

# ── VS Code generated files ───────────────────────────────
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch

# ── Secrets / Credentials ─────────────────────────────────
# NEVER commit actual WiFi credentials or API keys.
# Copy config.h.example → config.h and fill in your values.
include/config.h

# ── OS / Toolchain noise ──────────────────────────────────
.DS_Store
Thumbs.db
*.pyc
__pycache__/

# ── Firmware binaries (use Releases instead) ─────────────
*.bin
*.elf
*.hex
*.map

# ── Compiled firmware kept separately by release workflow ─
firmware/
